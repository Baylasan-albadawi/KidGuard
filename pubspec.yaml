name: kidguard
description: KidGuard - Child Safety Monitoring App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.7.0
  firebase_database: ^12.4.0
  firebase_auth: ^6.5.1
  firebase_messaging: ^16.2.2
  flutter_local_notifications: ^18.0.0
  provider: ^6.1.1
  http: ^1.1.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
